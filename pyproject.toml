[project]
name = "brevitas-quantizers"
version = "0.1.0"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["quantizers", "training_harness", "models", "utils", "importance"]
